# Screenshot provenance

These are unedited screenshots of Media Center v0.7.0, captured by the repository's desktop validation scripts on Windows. Music, audiobook, and podcast screenshots use the app's explicitly labeled sample collection. The immersive player uses controlled playback fixtures with original test lyric text. The customization image shows the real Settings controls during a test session. No personal library data, credentials, or third-party cover images are included.

The app mark is copied from `build/icon.svg`. All assets are part of this GPL-3.0-only project. Screenshots illustrate the interface; media is not bundled.

To refresh: run the desktop checks with `MPV_TEST_PATH` and the packaged executable as documented in the main README, visually review the resulting images in `artifacts/`, then copy only the selected screenshots here. Do not publish app profiles, logs, server addresses, or private library screenshots.
